feat: validacion inline formularios admin + InputField reutilizable

Componentes reutilizables (InputField.tsx):
- InputField, SelectField, TextAreaField, InputError
- ARIA (aria-invalid, aria-describedby, role=alert)
- Red border on error, dark mode, animate-fade-in

FormularioEmpleado:
- Migrado a InputField/SelectField con react-hook-form + zod

NuevaOrden:
- Validacion inline: proveedorId, fecha, notas, items
- Touch tracking + errores en vivo + dark mode

RecepcionMercancia:
- Validacion inline por lote (codigoLote, fechaVencimiento, cantidad)
- Touch tracking individual por campo/lote
- Dark mode completo
