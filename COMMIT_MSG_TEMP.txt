feat: PWA install analytics — beforeinstallprompt + banner + localStorage tracking

- Hook usePWAInstall: captura beforeinstallprompt, expone install(), dismiss(),
  analytics en localStorage (timestamps + conteos de banner, clics, instalaciones)
- InstallPWABanner: slide-up animado con botones Instalar / Ahora no, dark mode
- Integrado en PublicLayout para toda la tienda
- Fix de double-conteo con hasRecordedInstallRef (appinstalled + userChoice)
- Exportado desde hooks/index.ts