import { createPlaceholderPage } from '../_shared'

export default createPlaceholderPage({
	title: 'Restablecer contraseña',
	description: 'Define una nueva contraseña usando el token de recuperación enviado a tu correo.',
	actionLabel: 'Volver al login',
	actionTo: '/login',
})
